<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Background (transparent) -->
  <rect width="512" height="512" fill="transparent"/>
  
  <!-- Network connections (branches) -->
  <g stroke="#1890ff" stroke-width="28" fill="none" stroke-linecap="round" stroke-linejoin="round">
    <!-- Main horizontal line -->
    <path d="M 130 256 L 410 256"/>
    <!-- Arrow head -->
    <path d="M 370 220 L 410 256 L 370 292" stroke-width="32"/>
    
    <!-- Branch to top-left -->
    <path d="M 180 256 Q 180 190, 140 140"/>
    
    <!-- Branch to top-center -->
    <path d="M 240 256 Q 260 200, 280 160"/>
    
    <!-- Branch to top-right (active) -->
    <path d="M 320 256 Q 350 200, 390 160"/>
    
    <!-- Branch to bottom-right (active) -->
    <path d="M 320 256 Q 350 310, 390 360"/>
  </g>
  
  <!-- Nodes (circles) -->
  <!-- Top-left node -->
  <circle cx="130" cy="130" r="48" fill="#1890ff"/>
  
  <!-- Top-center node -->
  <circle cx="280" cy="150" r="48" fill="#1890ff"/>
  
  <!-- Left-center node -->
  <circle cx="120" cy="256" r="48" fill="#1890ff"/>
  
  <!-- Top-right node (active with green center) -->
  <circle cx="400" cy="150" r="52" fill="#1890ff"/>
  <circle cx="400" cy="150" r="24" fill="#52c41a"/>
  
  <!-- Bottom-right node (active with green center) -->
  <circle cx="400" cy="370" r="52" fill="#1890ff"/>
  <circle cx="400" cy="370" r="24" fill="#52c41a"/>
</svg>
